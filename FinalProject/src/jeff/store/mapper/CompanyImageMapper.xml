<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <mapper namespace="jeff.store.mapper.CompanyImageMapper">
 	
 	<insert id="insertCompanyImage" parameterType="CompanyImage">
 	<selectKey keyProperty="companyImageId" resultType="int" order="BEFORE">
    		select reportReview_tb_seq.nextval FROM DUAL
  		</selectKey>
 		insert into companyImage_tb(companyImageId, comId, fileName, contentType)
 		values(#{companyImageId}, #{comId}, #{fileName}, #{contentType})
 	</insert>
 	
 	<delete id="deleteCompanyImage" parameterType="int">
 		delete from companyImage_Tb
 		where companyImageId = #{companyImageId}
 	</delete>
 	
 	<select id="searchCompanyImageList" parameterType="String" resultType="CompanyImage">
 		select companyImageId, comId, fileName, contentType
 		from companyImage_tb
 		where comId=#{comId}
 	</select>
 
 </mapper>